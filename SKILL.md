---
name: ai-news-social-pipeline
description: 把每日 AI 新闻转成微信公众号深度稿 + 抖音长图文的完整可执行流水线（选题评分 → 事实核查 → 排版 → 出图 → 自动发草稿/发布 → 合规审查）。当用户需要从当天 AI 动态里挑出争议性最强的一条，产出可直接发布的抖音口播稿/分镜表/长图文正文和公众号文章（Markdown + 微信兼容 HTML + 封面），或需要做热点选题评分、自媒体合规审查、每日定时内容生产时使用。触发词：AI日报转内容、新闻做抖音、新闻写公众号、自媒体流水线、热点选题、争议性选题、每日内容生产、AI新闻二次创作、把这条新闻做成视频、自动发草稿、自动发抖音、定时推文。
agent_created: true
display_name: 每日 AI 新闻转公众号 / 抖音内容流水线
display_name_en: Daily AI News to WeChat & Douyin Pipeline
description_zh: 从每日 AI 新闻里挑出争议性最强的一条，产出公众号深度稿与抖音长图文，并自动推草稿箱 / 发布；含六维选题评分、回源事实核查、Pillow 出图与合规审查。
description_en: Turn daily AI news into a WeChat deep-dive article and a Douyin long-form post — six-dimension topic scoring, source-verified fact-checking, Pillow-generated visuals, and one-command publishing to the WeChat draft box / Douyin.
category: writing
version: 2.0.0
author: 陈博皓（89757-creater）
---

# AI 新闻 → 公众号 / 抖音 内容流水线

把「当天 AI 新闻」加工成「可发布的自媒体成品」，并**自动落到平台的草稿箱 / 发布位**。

**核心原则**：一条新闻只做一条内容。挑了争议性最强的，就别贪心做三条——深度比数量值钱。

> **v2.0.0 说明**：v1 是计划态文档（只产稿、发布靠人手）。本版按 2026-09-14 起连续 10 天的
> 实跑经验重写，**已打通两条自动落地链路**：公众号自动推草稿箱、抖音自动发布长图文。
> 与 v1 最大的差别见「能力边界」一节——**先读那节，再动手**。

---

## 0. 能力边界（★ 先读这一节，避免把「已存草稿」说成「已发布」）

| 链路 | 能做 | 做不到（硬天花板） |
|---|---|---|
| 微信公众号 | 写稿 → 排版 → 上传素材 → **自动进草稿箱**（`draft/add`） | **群发**（`freepublish/submit` 返回 `48001`）。微信自 2025-07 起回收个人主体账号发布类接口权限，改不了 |
| 抖音 | 写稿 → 出图 → 自动填表 → 上传头图/封面 → 选配乐 → 加话题 → **自动发布长图文** | 「文章」体裁**正文不支持插图**（只有头图 + 封面两个图位）；**已发布作品无编辑入口**——配乐、头图、封面必须在发布那一刻设好，事后补不了 |
| 抖音配额 | — | 「长图文」**每账号每日上限 10 次**，超限服务端返回 `status_code=536` |

**表述纪律（违反即为事故）**：
- 进草稿箱 ≠ 已发布。**任何交付都不得把「已进草稿箱」写成「已发布 / 已推送」。**
- 抖音发布成功判据是**读 `create_v2` 接口响应**（`{"status":200,...}` → `status_code=0`），
  **不是**页面文案——点发布后编辑器标签会跳管理页，页面文案会误导成「结果不明」。
- 缺凭证时**没生成草稿**，准确说法是「没生成草稿」，不是「生成了但没进草稿箱」。

---

## 1. 何时使用

- 需要从每日 AI 新闻里选选题，产出公众号 + 抖音内容，并（可选）自动落地。
- 需要给热点做「话题性 / 争议性」打分与筛选。
- 需要把已有新闻素材转成口播稿、分镜表、长图文正文、公众号深度稿。

**不适用**：从别人的爆款视频仿写（用 `douyin-transcript-imitation`）；纯标题生成；非 AI 领域新闻。

**配套技能**：
- 浏览器自动化底座 → `cdp-browser-automation`（抖音链全部依赖它）
- 公众号深度稿细则 → `wechat-deep-article`
- 敏感词词表 → `douyin-transcript-imitation/scripts/sensitive_check.py`

---

## 2. 默认参数（用户未指定时直接采用）

| 项 | 默认值 |
|---|---|
| 每日产出 | 1 条精选深度（深挖 1 个争议点，不铺开） |
| 公众号落地 | **自动推草稿箱**（主题 `redwhite`）；粉丝可见的群发由用户手点 |
| 抖音落地 | **自动发布长图文**（默认停在发布前，需显式加发布开关） |
| 配乐（抖音） | 优先级 **R&B → 爵士 → 后朋克**（用户 2026-09-21 指定，长期有效） |
| 素材来源 | 工作区根目录当日 `ai-daily-YYYY-MM-DD.html`；无则重新检索 |
| 产出目录 | 工作区 `social/YYYY-MM-DD/` |
| 字数 | 公众号 1500–2500 中文字；抖音口播稿 600–900 字（90–150 秒） |

---

## 3. 环境准备（每次开跑前 30 秒搞定）

### 3.1 运行时路径（**Bash 的 PATH 不可靠，一律用绝对路径**）

```bash
export PATH="/usr/bin:/bin:/usr/local/bin:$PATH"   # ★ 不设这句，ls/cat/grep/head 全部 not found
export PYTHONUTF8=1 PYTHONIOENCODING=utf-8         # 中文路径与文本 IO
PY="C:/Users/suge0/.workbuddy/binaries/python/envs/default/Scripts/python.exe"
NODE="C:/Users/suge0/.workbuddy/binaries/node/versions/22.22.2-3/node.exe"
SKILL="C:/Users/suge0/.workbuddy/skills/ai-news-social-pipeline"
WS="<当前工作区绝对路径>"
```

- ⚠️ 给 Windows 原生程序（`python.exe` / `node.exe`）传路径必须用 `C:/...`；
  `/c/...`（Git Bash 风格）会被解析错。
- ⚠️ `agent-browser` 在本机**不可用**（装不了 Chromium）→ 抖音一律走 CDP 直连。

### 3.2 工具链归属（★ 一条容易吃大亏的规矩）

**运行时的唯一入口是「工作区自有副本」`<WS>/tools/`，不是本 skill 内的目录。**

原因：skill 目录会被 marketplace 更新覆盖，本地补进去的文件会**静默消失**
（2026-09-19 实测：`wechat-deep-article/scripts/preflight.py` 就是这样没的）。
本 skill 内的 `tools/` 是**可移植分发版与归档**，用于：换新工作区时一键铺开、以及 GitHub 备份。

所以：
- **新工作区首次使用** → 把 `<SKILL>/tools/` 整体复制到 `<WS>/tools/`，再开跑。
- **日常运行** → 只用 `<WS>/tools/`，不要跨目录混用，否则改了一份另一份没改。

### 3.3 凭证位置（★ 绝不入库、绝不贴进对话）

| 用途 | 文件 |
|---|---|
| 公众号 | `<WS>/tools/wechat/.env`（AppID / AppSecret） |
| 抖音 | 登录态在浏览器 profile `<WS>/_edge_profile/`（无需任何密钥文件） |

`publish_wenyan.py` 会自动兜底加载 `.env`（环境变量优先）。
仓库里只保留 `<SKILL>/tools/wechat/.env.example`，**真 `.env` 一律 gitignore**。

---

## 4. 五阶段流程

### 阶段 0 · 取素材（先拿料，别先动笔）

1. 在工作区根目录找当日 `ai-daily-YYYY-MM-DD.html`，读其中的条目。
2. 找不到就把日期往前推 1 天；仍找不到则用 WebSearch 重新检索当日 AI 动态
   （AI Coding、具身智能、大模型、AI 政策）。
3. 整理候选清单，每条记：**事件一句话 + 硬数据 + 信源**。信源不足两条的直接淘汰。

### 阶段 1 · 选题评分（决定今天写什么）

对每条候选按 6 个维度打分（各 0–5 分），细则见 `references/controversy-scoring.md`：

- **冲突性** —— 是否存在明确对立面（公司 vs 公司、技术与伦理、官方口径 vs 实际结果）
- **认知反差** —— 是否反直觉（越强越糟、越便宜越贵、官方先说 A 又改口 B）
- **切身相关** —— 是否影响普通人的工作、钱包、隐私或安全感
- **讨论热度** —— 技术圈 / 社媒是否已经在争论
- **可验证性** —— 是否有硬数据或原始信源支撑
- **合规风险**（负分项）—— 是否触碰红线，命中即扣 3 分

**选择规则**：取总分最高的一条。若最高分 < 18 分，说明当天没有值得深挖的题，
出一份「当日选题简报」说明原因，**不硬凑内容**。

产出 `01_选题评分.md`：候选清单 + 各项得分 + 选定理由 + 备选一条。

**★ 追加一格「疯传抓手」**：写明这条选题天然自带哪几条原则、缺哪几条需要靠写法补足。
评分回答「值不值得写」，疯传抓手回答「写出来会不会被转」。缺口处理见
`references/writing-calibration.md`——**缺口如实标注，不换选题、不编案例凑**。

**★ 防撞题**：先看前 1–2 天的 `social/<date>/01_选题评分.md`，确认今天的角度没和昨天撞。
（2026-09-22 与 09-23 曾因未查重而都瞄向「成本」类角度，被迫临时换角度。）

### 阶段 2 · 公众号深度稿

产物：`02_公众号稿.md`（源）+ `02_公众号稿.html`（微信兼容）+ `cover.png` + `assets/` 正文图。

#### 第一步 · 回源核查（硬要求）

8 条硬规则见 `references/fact-check-rules.md`。核心几条：

- 禁止把自己的推算写成事实；凡算式要么引原文、要么改定性表述。
- 多源时刻打架时退到日期粒度，不写具体时刻。
- 孤证细节宁删不留。
- 媒体口径与官方文件口径不一致时**并列**呈现，或改定性（如「近半数」）。
- **主动找「反转 / 打脸」细节**——官方先说 X 几小时后改口 Y，这往往是最好的钩子。
- 优先挖当事人的自嘲、调侃原话：零争议且有传播力。

#### 第二步 · 结构（1500–2500 中文字）

```
开场钩子（一个具体场景或一句反差原话，不要「随着…的发展」）
→ 01 发生了什么（事实层，带数据与信源）
→ 02 争议在哪（把两边主张都摆出来，不预设立场）
→ 03 这对普通人意味着什么（落到工作 / 钱包 / 隐私）
→ 结语（给判断，但标明是判断）
→ 评论区引导
→ 数据来源说明
→ 免责声明
```

#### 第三步 · 排版（★ 推荐用脚本，别手写 HTML）

```bash
"$PY" "$SKILL/scripts/md2wx.py" "$WS/social/<date>/02_公众号稿.md" \
      -o "$WS/social/<date>/02_公众号稿.html" \
      --accent "#34527A" --banner "#F2F5FA"
```

`md2wx.py` 已内置全部排版铁律（纯 inline style / 只用 `background-color` /
小标题中性深色 + 4px 左竖线 / `td` 显式 color / 半角引号转全角）。
手写 HTML 是返工的主要来源，**能用脚本就用脚本**。

如需手改，铁律见 `references/platform-specs.md`。

#### 第四步 · 自检（每日必跑，两关）

```bash
# 关 1：HTML 排版自检（通用铁律 + 结构 + td color）
"$PY" "$SKILL/scripts/check_html.py" "$WS/social/<date>/02_公众号稿.html" \
      --title "<标题>" --expect "关键词1" "关键词2"

# 关 2：发布前自检（新浪围栏：字节数 / 图片数 / 文件存在）
"$PY" "$WS/tools/wechat/scripts/preflight.py" "$WS/social/<date>/02_公众号稿.md"
```

- `--expect` 传当天稿件里的**关键数字**，不要写死在脚本里
  （旧版把某日关键词硬编码，换选题后必然 FAIL——那是脚本缺陷，不是稿件问题）。
- `preflight` 的 `--min-images` 默认 2，**正文图不足 2 张会被判不通过**（画面单薄）。
- 字数先按 **CJK 字符**计数，`publish_wenyan.py --verify` 报的「中文字」会比 CJK 再低 15–20 字，
  **留 50 字余量最稳**。「数据来源」段最容易失控——每条压到一句话。

#### 第五步 · 推送草稿箱

```bash
"$PY" "$WS/tools/wechat/scripts/publish_wenyan.py" \
      "$WS/social/<date>/02_公众号稿.md" --theme redwhite --verify
```

失败先跑这一条定位，**一步分清是密钥问题还是 IP 问题**：

```bash
"$PY" "$WS/tools/wechat/scripts/publish_wenyan.py" --check-credentials
# 打印掩码 / 载入路径 / 就绪态 + 实时出口 IP，不碰微信接口
```

错误码 | 含义 | 处置：
- `40164` / `61004` → **出口 IP 漂移**（家庭宽带动态 IP）→ 让用户加白名单，本轮只交付 HTML
- `40125` → **AppSecret 失效**（平台侧重置即旧值作废，本地 `.env` 看不出异常）
- `40243` → AppSecret 被冻结；`40013` → AppID 无效；`48001` → 无权限（群发，改不了）

凭证恢复入口（**路径必须带 `/platform/`**）：
`https://developers.weixin.qq.com/platform/` → 我的业务 → 公众号 → 基础信息 → 开发密钥
（此处可同时改 AppSecret 与 IP 白名单）。
⚠️ 裸域名是开放社区资讯站（没有「我的业务」）；老后台 `mp.weixin.qq.com → 设置与开发`
官方已整体迁移——这两条路都别再走（2026-09-21 已踩）。
详细流程见 `tools/wechat/凭证恢复操作流程.md`。

### 阶段 3 · 抖音

产物：`03_抖音口播稿.md` + `04_抖音分镜表.md` + `douyin/抖音版文案.md` + 竖版封面/头图。

#### 3.1 口播稿与分镜表

**口播稿结构**（600–900 字，90–150 秒）：

```
黄金 3 秒钩子（一句反常识结论或一个扎眼数字）
→ 事件速述（20 秒内讲清发生了什么）
→ 争议拆解（把冲突双方摆开，这是完播率的核心）
→ 我的判断（明确表态，别和稀泥）
→ 互动引导（问一个具体问题，不要「你怎么看」）
```

- 口语化：短句、能一口气读完、避免书面词（「此外」「综上」一律不要）。
- 数字必须来自阶段 2 核实过的口径。
- 每 15–20 秒设一个「留人点」（悬念、反转、金句）。
- **不得照抄**公众号稿段落；两套平台读法不同，要重写。
  实测纪律：两稿连续重合文字 **≤20 字**（更早一版曾出现 45 字连续重合，被迫重写）。

**分镜表字段**：镜号 / 画面描述 / 口播对应 / 字幕重点 / 时长。
画面描述要能直接指导拍摄（**景别 + 动作 + 道具**），不写「配合画面」这类空话。

#### 3.2 抖音版文案（★ 格式是硬约束）

文件名固定 `<date>/douyin/抖音版文案.md`，**必须用二级标题分节**：

```markdown
## 标题
<≤30 字>

## 摘要
<≤40 字>

## 正文
<段落，段间空行>
```

⚠️ **写成 `**标题**` 粗体行会直接报 `IndexError: list index out of range`** ——
`douyin_fill.py` 按 `^## ` 解析，这是 2026-09-23 实测踩过的坑。
话题清单一并写在这个文件里（≤5 个），正文与话题之间用 `##` 分隔，别紧贴正文段尾。

#### 3.3 视觉出图（Pillow，禁用 AI 绘图）

公众号封面 900×383 / 正文图 1200×620 / 抖音竖版封面与头图 1080×1440。

```bash
# 复制当日模板后改文案（★ 图内文案是写死的，换题必须改脚本）
cp "$SKILL/scripts/gen_assets_example.py" "$WS/social/<date>/scripts/gen_assets.py"
"$PY" "$WS/social/<date>/scripts/gen_assets.py"
```

**为什么只用 Pillow**：`cairosvg` 在本机装不上（缺 `libcairo`），
**任何 SVG→PNG 方案都不可用**；Pillow 版是等效替代，字体走 `C:\Windows\Fonts` 的微软雅黑。

出图后**必跑两道自检**（`gen_assets_example.py` 已内置）：
1. **调色板自检**：色相间隔 ≥25°、RGB 欧氏距离 ≥12、深底亮度均值 <60。
   ⚠️ **别手算色相**——人脑估算极易错（2026-09-23 手算 127.7° 实际 112.3°，
   导致自检 FAIL、白跑一轮）。**信脚本输出，不信估算。**
2. **边缘溢出检查**：扫四边 12px 内是否有非底色像素，确认文字没出画布。

#### 3.4 自动发布长图文（CDP 直连 Edge，无官方 API）

**准备：拉起带调试端口的 Edge，并保持常驻**

```bash
# ★ 必须用 run_in_background 起；前台启动的 msedge 会被沙箱收割
bash "$WS/tools/edge_keepalive.sh"
# 探活：
curl -s --noproxy '*' http://127.0.0.1:9222/json/version
```

- 端口 `9222`，独立 profile `<WS>/_edge_profile`，登录态在里面。
- 入口页：`https://creator.douyin.com/creator-micro/content/post/article?default-tab=5`

**生产链（按顺序，每步都可单跑校验）**

| 步 | 脚本 | 要点 |
|---|---|---|
| 1 | `douyin_fill.py` | 填标题/摘要/正文。**重填必须加 `--clear`**（默认是末尾追加，会越填越长） |
| 2 | `douyin_upload2.py` | 头图 + 封面。头图入口文案是「**点击替换图片**」；头图弹窗点「确定」、封面弹窗点「完成」 |
| 3 | `douyin_music_pick.py` | **配乐唯一入口**。规则见下 |
| 4 | `douyin_topic.py` | 加话题，**≤5 个**。先读 `topicCount`：已是 5/5 就别重跑（重跑时「添加话题」弹窗可能连开 5 次都打不开） |
| 5 | `douyin_post_article.py` | 编排全链，**默认停在发布前** |
| 6 | `douyin_publish_probe.py` | 点发布并**读接口响应**判定结果 |

**配乐规则（用户 2026-09-21 指定，长期有效）**
- 原话：「不用纯音乐也可以，但风格要求 rnb、爵士、后朋克」→ ① 不再限定「纯音乐」页签；
  ② 优先级 **R&B / 爵士 / 后朋克**。
- 面板无风格类页签 → 只能走 `input[placeholder="搜索音乐"]`（`douyin_music_pick.py --search`）。
- 实测存量：爵士 11 条、R&B 31 条；⚠️ **后朋克基本不可得**（仅 2 条真命中且均 0 人使用，
  其余是「蒸汽/赛博朋克」误命中）——**曲库问题，缺口如实标注，不拿赛博朋克冒充**。
- 常用曲：`r&b loop`、`乌鸦的爵士咖啡馆`、`夜上海`。

**★ 发布前必做的配额闸门（最容易吃大亏的一步）**

```bash
"$PY" "$WS/tools/quota_ledger.py"          # 看今日已用 / 是否放行
"$PY" "$WS/tools/quota_ledger.py" --mark "$(date +%F)" --note "发布成功"
```

「删除 + 重发」是换配乐/换封面的**唯一手段**（发布后无编辑入口），
一旦先删旧作、重发又被配额拒，就会出现「旧作已删、新作没发出去」的空窗
—— 2026-09-18 就是这样静默丢了一篇（作品数 5 → 4）。

**三条铁纪律**：
1. **删前先过 `quota_ledger.py` 闸门**（台账只是**下界**，它数到 10 时才具确定性）。
2. **发布结果一律读 `create_v2` 接口响应**，别读页面文案
   （超限时页面只闪 **0.6 秒**的 toast，sleep 后再读什么都读不到 → 会误判「结果不明」）。
3. **见 rc=4 立刻停手**（服务端已拒，重试无效），等额度 00:00 重置后 `--no-delete` 补发。
   ⏳ 已知未实现：「先发新的、成功后再删旧的」——**目前没有这个安全顺序**，故删旧务必过闸门。

**风控与降级**：点「发布」可能触发短信验证风控（2026-09-15 触发、09-16 直接成功，**非必然**）
→ 先正常点，真弹框再降级：取消弹窗 → 暂存离开存草稿 → 交付写明「已存草稿，需用户手机端过验证」。
**不要填验证码**（收不到；乱填会升级为扫码风控）。

### 阶段 4 · 配图提示词

产物 `05_配图提示词.md`（公众号封面 + 首图 + 抖音竖版封面）。

按 `design-image-prompt-engineer` 的结构写：主体 / 环境 / 光线 / 镜头 / 风格 / 后期，各一段，
再附反向提示词。要点：
- 同一选题的封面与正文配图**共用一套色板**，与阶段 2 的主题色同源。
- 提示词里不出现真实品牌 Logo、水印、真人肖像。
- 需实际出图时用 ImageGen，**生成会消耗额度，先告知用户**。
  （日常流水线的图**走 Pillow 程序化生成**，不消耗额度，见 3.3。）

### 阶段 5 · 合规审查与交付

1. **敏感词检测**（必跑，见 `references/pitfalls.md` 的空跑陷阱）：

```bash
"$PY" "$SKILL/scripts/check_douyin_sensitive.py" "$WS/social/<date>/03_抖音口播稿.md" \
      --cut "## 留人点清单"
```
   命中词必须改，并在文件末记录「命中词 → 处理结果」。
   实测高频误伤：**「最终 / 最后」含「最」、「第一轮」含「第一」、「付费」属诱导互动、「二维码」也中**。

2. 过一遍红线自查（见第 6 节）。
3. 用 `present_files` 一次性交出全部产物，按阅读顺序排列。
4. 追加当日 memory：选题、得分、产出文件清单、命中的敏感词、降级说明。

---

## 5. 输出目录规范

```
social/YYYY-MM-DD/
├── 01_选题评分.md        # 含「疯传抓手」一格
├── 02_公众号稿.md        # frontmatter: title / description / cover:./cover.png
├── 02_公众号稿.html      # md2wx.py 产出
├── 03_抖音口播稿.md
├── 04_抖音分镜表.md
├── 05_配图提示词.md
├── cover.png             # 900×383
├── assets/               # 正文图 1200×620 ×2+
├── scripts/              # 当日出图脚本 + 三个自检脚本副本
└── douyin/
    ├── 抖音版文案.md      # ★ 必须 ## 标题 / ## 摘要 / ## 正文
    ├── douyin_cover.png  # 1080×1440
    └── douyin_head.png   # 1080×1440
```

完整样例见 `<SKILL>/examples/2026-09-23/`（可直接对照格式）。

---

## 6. 红线自查（发布前必过）

- 不用「中国赢麻 / 中美对决 / 国产 vs 美国」这类对立框架，改中性叙事（如「出海路径变化」）。
- 外企、军事、政治相关的题只写技术与商业事实，不延伸评价。
- 不点名具体竞品型号做对比，用「某主力模型」兜底。
- 涉及港澳台一律写「中国香港 / 中国台湾 / 中国澳门」。
- 医疗题：机器人 ≠ 主刀，分工要钉死。
- 不编造销量、荣誉、检测数据、用户评价；信息缺失标注 `[待核实]` 或直接不提。
- 不承诺涨粉、转化、爆款结果。
- 对外动作（发布）前**先过配额闸门与凭证自检**；拿不准就先降级为草稿并明说。

---

## 7. 疯传六原则（用户 2026-09-16 指定，长期有效）

> 用户原话：「以后写公众号与抖音文稿时采取图中原则」——附图即《疯传》（Contagious）的六原则。
> **写法层的硬约束，与选题评分（值不值得写）是两回事**：评分挑题，六原则保证写出来会被转。

| 原则 | 文稿里怎么落 |
|---|---|
| **社交货币** | 给读者一个「转出去显得我懂行」的点：反直觉的机制解释、行业内门槛知识。**用「多数人还不知道」替代说教** |
| **诱因** | 把话题挂到读者高频接触的日常场景（每天打开编辑器、每次点「批准」、每月收账单） |
| **情绪** | **优先高唤醒**：惊奇 > 自豪 > 愤怒 > 焦虑。低唤醒的「满足」「忧伤」不上钩 |
| **公开性** | 把看不见的东西做成看得见的——账单上的倍率、授权范围、圈内人的选择，给读者一个能指给别人看的具象物 |
| **实用价值** | 必须有**可转述、可执行**的一段：具体怎么做、去哪看、问谁一句 |
| **故事** | 观点藏在故事里。**开场带具体场景与人物动作**，别用「随着…的发展」开门 |

**三条硬规矩**：
1. **一篇至少踩中 4 条**，且必须含「社交货币」或「实用价值」之一 —— 只有情绪没有用处的稿子是烟花。
2. **情绪项优先高唤醒**；写不出激动 / 惊奇 / 愤怒 / 焦虑中的任一种，就回炉改钩子。
3. **公号与抖音各自独立踩**，不是同一份稿贴两次：
   - 公众号 → 偏「社交货币 + 故事 + 实用价值」（读得慢，要经得起转给懂的人看）
   - 抖音 → 偏「情绪（高唤醒）+ 诱因 + 故事」（黄金 3 秒制造惊奇或愤怒）

**缺口处理（用户 2026-09-16 拍板）**：某条踩不上或素材撑不起来（最常见是「故事」缺可核实案例），
**如实标注缺口，不换选题、不编案例凑**。在 `01_选题评分.md` 写明哪条弱、为什么弱。
唯一例外：缺口能靠**已有真实素材**补足——判断标准是「这个细节能回源到至少一条信源」，
不是「这样写会不会更好看」。**真实比热闹重要。**

细则见 `references/writing-calibration.md`。

---

## 8. 资源索引

### references/
| 文件 | 内容 |
|---|---|
| `controversy-scoring.md` | 六维评分细则与判定示例 |
| `fact-check-rules.md` | 事实核查 8 条硬规则 + 排版自检正则避坑 |
| `platform-specs.md` | 公众号 / 抖音格式硬约束速查 |
| `writing-calibration.md` | 疯传六原则落地写法与自查清单 |
| `wechat-publish.md` | 公众号发布链路、错误码、凭证恢复 |
| `douyin-publish.md` | 抖音 CDP 链路、配额、配乐、产品边界 |
| `troubleshooting.md` | 故障排查速查表（现象 → 原因 → 处置） |
| `pitfalls.md` | 已踩坑清单（每条都附实测日期，避免重复踩） |

### scripts/（通用，跨选题复用）
| 文件 | 用途 |
|---|---|
| `md2wx.py` | Markdown → 微信兼容 HTML（内置全部排版铁律） |
| `check_html.py` | HTML 排版自检（通用铁律 + 结构 + td color） |
| `check_douyin_sensitive.py` | 抖音敏感词检测（**带阳性对照**，防空跑假阴性） |
| `gen_assets_example.py` | Pillow 出图模板（封面 + 正文图 + 抖音竖版，含调色板/溢出双自检） |
| `gen_cover_pillow.py` | 公众号封面单出（900×383） |
| `gen_diagrams_pillow.py` | 正文配图单出（1200×620） |

### tools/（平台链路，★ 运行时应使用工作区副本）
| 目录 | 内容 |
|---|---|
| `tools/douyin_*.py` | 抖音生产链：fill / upload2 / topic / music_pick / post_article / publish_probe |
| `tools/` 维护链 | `douyin_delete.py`、`republish_one.py`、`quota_ledger.py`（删前闸门） |
| `tools/` 诊断链 | `state_check.py`、`editor_probe.py`、`cdp_read.py`(CDP 底座) |
| `tools/edge_keepalive.sh` | 保活拉起带 9222 调试端口的 Edge |
| `tools/wechat/` | 公众号链：`scripts/preflight.py`、`scripts/publish_wenyan.py`、`themes/*.css` |
| `tools/push_to_github_api.py` | **元工具**：走 REST API 推送本仓库（绕开被阻断的 `github.com` 传输通道） |

完整工具索引（含「哪些是早期版本、不要用」）见 `tools/README.md`。

### examples/
`examples/2026-09-23/` —— 一份完整实跑产出，可直接对照格式与语气。

---

## 9. 已知局限（诚实清单，别当成已解决）

- **抖音「先发新的、成功后再删旧的」顺序尚未实现**，删旧仍有过期风险窗口。
- `quota_ledger.py` 只记录本工具的成功发布次数，手工发的看不到 → 它给出的余量只是**下界**。
- 后朋克配乐在曲库里基本不可得（存量 2 条、均 0 人使用）。
- 抖音「文章」正文不能插图，选题若强依赖多图论证，需改用视频体裁（另一条链路，未沉淀）。
- `tools/douyin_fill.py` 等脚本内有硬编码的工作区绝对路径（`WS = Path(r"C:/Users/...")`），
  **换机器/换工作区需改这两行**。这是已知的可移植性缺口，不是文档遗漏。
- 本 skill 目录会被 marketplace 覆盖 → 新增工具请同时落 `<WS>/tools/` 与 Git 仓库。
- 本机 `github.com` 主站不稳定（代理 `CONNECT tunnel failed, 502`，curl 返回 000）
  → **`git push` 不可靠**；同步请用 `tools/push_to_github_api.py`（走 `api.github.com`，实测稳定）。
