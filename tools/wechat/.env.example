# tools/wechat/.env —— 公众号发布凭证
#
# 使用方法：把本文件复制为同目录下的 `.env`（无 .example 后缀），填入真实值。
# publish_wenyan.py 在环境变量不全时，会自动兜底加载这个文件。
# 查找顺序：CWD/.env → scripts/.env → tools/wechat/.env（本文件所在处）
#
# ⚠️ 注意
#  - 环境变量优先于 .env：两者同时存在时以环境变量为准。
#  - 不要把真实凭证提交到任何仓库，也不要贴进聊天窗口。
#  - 光有凭证还不够：调用方出口 IP 必须在公众号后台「设置与开发 → 安全中心 → IP 白名单」内，
#    否则 API 返回 40164。出口 IP 可用 https://ipinfo.io/ip 查。

WECHAT_APP_ID=wx0000000000000000
WECHAT_APP_SECRET=00000000000000000000000000000000
