# 行尾统一为 LF，避免跨平台问题。
# 本机 core.autocrlf=true，若不显式约束，shell 脚本会被 CRLF 化，
# 在 Git Bash / Linux 下报 "\r: command not found"。

* text=auto eol=lf

# 可执行脚本必须 LF
*.sh  text eol=lf
*.py  text eol=lf
*.md  text eol=lf
*.json text eol=lf
*.yaml text eol=lf
*.yml  text eol=lf
*.css  text eol=lf

# 二进制，不做行尾转换
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.zip  binary
